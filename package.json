{
  "name": "ai-assistant",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-level E2E automation for the AI Assistant dashboard",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "check:web": "node --check apps/web/assets/dashboard_runtime.js && node --check apps/web/assets/dashboard_task_utils.js && node --check apps/web/assets/dashboard.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
